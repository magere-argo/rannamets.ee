---
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container site-footer__inner">
    <span class="site-footer__copy">© {year} Magere OÜ</span>
    <nav class="site-footer__links" aria-label="Footer">
      <a class="site-footer__link" href="/privacy/">Privacy</a>
      <a class="site-footer__email" href="mailto:argo@rannamets.ee">argo@rannamets.ee</a>
    </nav>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--color-bg);
    border-top: var(--hairline) solid var(--color-border);
    padding-block: var(--space-6);
    margin-top: var(--space-16);
  }

  .site-footer__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  .site-footer__copy {
    font-family: var(--font-heading);
    font-size: var(--text-meta);
    letter-spacing: var(--tracking-button);
    color: var(--color-slate-400);
  }

  .site-footer__links {
    display: flex;
    align-items: center;
    gap: var(--space-6);
  }

  /* Subordinate utility link (Privacy) — keeps the email as the primary
     contact CTA. Meta scale, copper on hover, no permanent underline. */
  .site-footer__link {
    font-family: var(--font-heading);
    font-size: var(--text-meta);
    letter-spacing: var(--tracking-button);
    color: var(--color-slate-400);
    text-decoration: none;
  }
  .site-footer__link:hover {
    color: var(--color-copper);
  }

  .site-footer__email {
    font-family: var(--font-heading);
    font-size: var(--text-meta);
    font-weight: var(--weight-semibold);
    letter-spacing: var(--tracking-button);
    color: var(--color-navy);
    text-decoration: none;
    border-bottom: 1px solid var(--color-copper);
    padding-bottom: 1px;
  }
  .site-footer__email:hover {
    color: var(--color-copper);
  }
</style>
